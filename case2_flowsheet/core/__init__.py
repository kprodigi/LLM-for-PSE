# -*- coding: utf-8 -*-
"""
core — shared kernel for the two case studies of the paper
"Admissibility-Aware AI for Fail-Closed Decision Support in Process Systems
Engineering".

Holds ONLY genuinely shared, behaviour-identical components (or parameterized
bases that reproduce each case's exact behaviour). Components that have diverged
between the two case studies are intentionally NOT unified here — see
RECONCILIATION.md. Behaviour preservation takes precedence over de-duplication.
"""
